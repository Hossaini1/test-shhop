// import { fetchPosts } from "@/actions/FetchPost"

// export async function generateStaticParams() {
//   const posts = await fetchPosts({ limit: 1000 }) // oder pagination false, wenn du willst
//   return posts.map((post) => ({ slug: post.slug }))
// }